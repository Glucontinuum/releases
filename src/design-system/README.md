# Design System snapshot

This directory is a checked-in, versioned snapshot of the generated Web output. It is updated only by a pull request that changes design-system/version.json and the generated files together.

The canonical source is Glucontinuum/glucontinuum-design-system. This repository owns only the integration and the product-specific mapping.
