## Agent skills

### Issue tracker

Issues and specs live in this repository's GitHub Issues; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Use `policy:needs-triage`, `question`, shared `ready`, and `wontfix` as mapped in `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository; read the root `CONTEXT.md` and relevant `docs/adr/` files. See `docs/agents/domain.md`.
